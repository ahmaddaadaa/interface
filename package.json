{
  "name": "mnist-fpga-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "MNIST FPGA dashboard + phone photo capture",
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && rm -rf server/public && cp -R client/dist server/public",
    "start": "NODE_ENV=production node server/index.js",
    "prod": "npm run build && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
